# Terminal Chat

A simple client-server based chat application. Multiple users can chat using
a single chat server. The application is multithreaded.

Usage:

```sh
# start server
python3 server.py <port-to-bind>

# start client(s)
python3 client.py <server-port> <name>
```
